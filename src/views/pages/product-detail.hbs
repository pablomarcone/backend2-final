<div class="container mx-auto px-4 max-w-6xl py-8">

    <div class="mb-6">
        <button onclick="history.back()" class="flex items-center text-gray-600 hover:text-gray-800 transition-colors">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
            </svg>
            Volver a la página anterior
        </button>
    </div>

    <div class="bg-white rounded-lg shadow-lg overflow-hidden">

        <div class="bg-gray-50 px-6 py-4 border-b">
            <div class="flex justify-between items-start">
                <div>
                    <h1 class="text-3xl font-bold text-gray-800 mb-2">{{title}}</h1>
                    <p class="text-gray-600">Código: <span class="font-medium">{{code}}</span></p>
                </div>
                <div class="text-right">
                    <div class="mb-2">
                        {{#if status}}
                        <span class="px-3 py-1 rounded-full text-sm bg-green-100 text-green-800 font-medium">
                            Producto Activo
                        </span>
                        {{else}}
                        <span class="px-3 py-1 rounded-full text-sm bg-red-100 text-red-800 font-medium">
                            Producto Inactivo
                        </span>
                        {{/if}}
                    </div>
                    <p class="text-sm text-gray-500">ID: {{_id}}</p>
                </div>
            </div>
        </div>

        <div class="p-6">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">

                <div>
                    <h2 class="text-xl font-semibold text-gray-800 mb-4">Imágenes del Producto</h2>

                    {{#if thumbnails}}
                    {{#if thumbnails.length}}
                    <div class="mb-4">
                        {{#each thumbnails}}
                        {{#if @first}}
                        <img id="main-image" src="{{this}}" alt="{{../title}}"
                            class="w-full h-96 object-contain rounded-lg border shadow-md bg-white"
                            onerror="this.src='data:image/svg+xml;base64,'">
                        {{/if}}
                        {{/each}}
                    </div>

                    <div class="grid grid-cols-4 gap-2">
                        {{#each thumbnails}}
                        <img src="{{this}}" alt="{{../title}} - Imagen {{@index}}" onclick="changeMainImage('{{this}}')"
                            class="thumbnail-img w-full h-20 object-contain rounded-md border cursor-pointer hover:border-blue-500 transition-colors bg-white {{#if @first}}border-blue-500{{/if}}"
                            onclick="changeMainImage('{{this}}')"
                            onerror="this.src='data:image/svg+xml;base64,'">
                        {{/each}}
                    </div>

                    {{else}}
                    <div class="w-full h-96 bg-gray-200 rounded-lg border flex items-center justify-center">
                        <div class="text-center text-gray-500">
                            <svg class="w-16 h-16 mx-auto mb-2" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd"
                                    d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z"
                                    clip-rule="evenodd"></path>
                            </svg>
                            <p>Sin imágenes disponibles</p>
                        </div>
                    </div>
                    {{/if}}
                    {{else}}
                    <div class="w-full h-96 bg-gray-200 rounded-lg border flex items-center justify-center">
                        <div class="text-center text-gray-500">
                            <svg class="w-16 h-16 mx-auto mb-2" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd"
                                    d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z"
                                    clip-rule="evenodd"></path>
                            </svg>
                            <p>Sin imágenes disponibles</p>
                        </div>
                    </div>
                    {{/if}}
                </div>

                <div>
                    <h2 class="text-xl font-semibold text-gray-800 mb-4">Información del Producto</h2>

                    <div class="mb-6">
                        <h3 class="font-medium text-gray-700 mb-2">Descripción</h3>
                        <p class="text-gray-600 leading-relaxed">
                            {{#if description}}
                            {{{description}}}
                            {{else}}
                            Sin descripción disponible.
                            {{/if}}
                        </p>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                        <div class="space-y-4">
                            <div>
                                <h4 class="font-medium text-gray-700 mb-1">Precio</h4>
                                <p class="text-2xl font-bold text-green-600">${{price}}</p>
                            </div>
                            <div>
                                <h4 class="font-medium text-gray-700 mb-1">Stock Disponible</h4>
                                <p class="text-lg {{#if stock}}text-gray-800{{else}}text-red-600{{/if}}">
                                    {{#if stock}}
                                    {{stock}} unidades
                                    {{else}}
                                    Sin stock
                                    {{/if}}
                                </p>
                            </div>
                        </div>
                        <div class="space-y-4">
                            <div>
                                <h4 class="font-medium text-gray-700 mb-1">Categoría</h4>
                                <span class="inline-block bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm">
                                    {{category}}
                                </span>
                            </div>
                            <div>
                                <h4 class="font-medium text-gray-700 mb-1">Código de Producto</h4>
                                <p class="text-gray-800 font-mono">{{code}}</p>
                            </div>
                        </div>
                    </div>

                    <div class="space-y-3">
                        {{#if status}}
                        {{#if stock}}
                        <button class="w-full bg-blue-600 text-white py-3 px-6 rounded-lg hover:bg-blue-700 focus:outline-none 
                            focus:ring-2 focus:ring-blue-500 text-lg font-medium transition-colors"
                            id="add-to-cart-btn" data-product-id="{{_id}}">
                            🛒 Agregar al Carrito
                        </button>
                        {{else}}
                        <button disabled
                            class="w-full bg-gray-400 text-white py-3 px-6 rounded-lg cursor-not-allowed text-lg font-medium">
                            Sin Stock Disponible
                        </button>
                        {{/if}}
                        {{else}}
                        <button disabled
                            class="w-full bg-gray-400 text-white py-3 px-6 rounded-lg cursor-not-allowed text-lg font-medium">
                            Producto No Disponible
                        </button>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="/js/product-detail.js"></script>